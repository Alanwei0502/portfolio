import {
  AppBar
} from '@mui/material';
import styled from '@emotion/styled';

export const HeaderContainer = styled(AppBar)`
box-shadow: none;
position: fixed;
background: transparent;
color: inherit;
`;